# jetbrains-youtrack

Run: `make init` to initialize the setup

Run: `make update` to get check if docker is installed and latest image is pulled

Run: `make start` to run the local server

Run: `make healthcheck` to test if server is ready